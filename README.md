! This README is currently in writing and not finished yet !

# Init project

* Add config file
`www/js/config.js`
Example config file:
```
var config = {
    api42 : {
        token: 'YOU_API42_TOKEN_HERE'
    }
};
```